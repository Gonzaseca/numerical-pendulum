{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Pendulum Test",
            "type": "debugpy",
            "request": "launch",
            "module": "experiments.test_pendulum",
            "cwd": "${workspaceFolder}"
        }
    ]
}